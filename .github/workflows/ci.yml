name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]
    env:
      QT_QPA_PLATFORM: offscreen
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          # libegl1/libgles2: Qt GUI (offscreen). libpulse0 + mainloop-glib:
          # required by PySide6.QtMultimedia (QMediaPlayer) at import time on Linux.
          sudo apt-get install -y libegl1 libgles2 libpulse0 libpulse-mainloop-glib0

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - run: pip install -e ".[dev]"
      - run: ruff check .
      - run: mypy src/
      - run: pytest --cov=opendance --cov-report=term-missing
